<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nbgsoftware.gbrains">

    <application
        android:allowBackup="true"
        android:icon="@drawable/thumbnail_splash_v2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/thumbnail_splash_v2"
        android:supportsRtl="true"
        android:theme="@style/Theme.GbrainsKotlinProject">
        <activity
            android:name=".ui.container.ContainerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>